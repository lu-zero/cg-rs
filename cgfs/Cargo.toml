[package]
name = "cgfs"
description = "safe cgroupfs v2 hierarchy, delegation, attachment, deletion, and control-file access"
keywords = ["cgroup", "cgroupv2", "cgroupfs", "linux"]
categories = ["os::unix-apis"]
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[features]
# Enables the explicit unsafe fake-root constructor used by dependent crate tests.
test-utils = []

[dependencies]
rustix = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]
